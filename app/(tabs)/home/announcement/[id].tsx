import React from "react";
import { ScrollView, Text } from "react-native";

export default function AnnouncementDetail() {
    return (
        <ScrollView>
            <Text>Détail de l'annonce</Text>
        </ScrollView>
    );
}
