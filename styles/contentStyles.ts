import { StyleSheet } from "react-native";

export const contentStyles = StyleSheet.create({
    container: {
        paddingHorizontal: 16,
    },
    contentFooter: {
        marginTop: 24,
        fontSize: 13,
        fontWeight: "500",
        textAlign: "center",
        color: "#a69f9f",
    },
});
